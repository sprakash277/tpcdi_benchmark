# TPC-DI Data Generation and Benchmark for Databricks/Dataproc
# 
# Core dependencies:
pyspark>=3.0.0

# For Databricks: pyspark and dbutils come from the runtime
# For Dataproc: pyspark comes from the cluster, GCS connector should be installed on cluster
#
# Optional for local development:
# google-cloud-storage>=2.0.0  # For GCS operations if needed

# Optional for Databricks SQL / Apps:
databricks-sql-connector
